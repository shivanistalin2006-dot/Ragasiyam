# Puzzle of the Day Assets Directory
- Vector SVG icons are dynamically synthesized in CSS/HTML for zero latency.
- Audio effects are synthesized in real-time using the Web Audio API synthesizer (`RetroSoundSynth` in `js/ui.js`).
